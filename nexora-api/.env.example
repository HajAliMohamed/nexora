# Database
DATABASE_URL=postgresql://nexora:nexora@localhost:5433/nexora

# Redis
REDIS_URL=redis://localhost:6380

# Auth
JWT_SECRET=change-me-to-a-random-secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS / cross-repo
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:3001

# SERP provider (google uses Puppeteer to scrape Google directly — free, no API key)
SERP_PROVIDER=google
SEARXNG_BASE_URL=http://localhost:8888

# Keyword research provider (autocomplete uses Google Suggest API — free, no key needed)
KEYWORDS_PROVIDER=autocomplete

# Performance
PAGESPEED_API_KEY=

# Stripe
BILLING_ENABLED=true
BILLING_PROVIDER=stripe
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder
STRIPE_PRO_PRICE_ID=price_pro_placeholder
STRIPE_AGENCY_PRICE_ID=price_agency_placeholder

# Admin test account (seed-admin endpoint)
ADMIN_EMAIL=admin@nexora.io
ADMIN_PASSWORD=changeme123

# V2 - AWS S3 for PDF storage (optional, falls back to local disk)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=

# V2 - Zeze Innovation Email API
ZEZE_API_KEY=sk_live_stageconnect_a8b3c9d2e7f4g5h6i7j8k9l0m1n2o3p4
ZEZE_PROJECT_ID=proj_stageconnect_prod

# SearXNG (self-hosted, for AI search visibility checks)
SEARXNG_API_URL=http://localhost:8888

# V2 - Groq LLM (free tier: 14,400 req/day)
# Sign up at https://console.groq.com for free API key
GROQ_API_KEY=your-groq-api-key

# V3 - ML Service (Python microservice)
ML_SERVICE_URL=http://localhost:5000
